# Optional audio-analysis stack.
#
# Install only when we are ready to run real audio feature extraction locally.
librosa
numpy
scipy
soundfile
sounddevice

# aubio is useful for realtime onset/beat work, but it can fail to build on
# macOS Apple Silicon with recent Python/NumPy. Install separately only when
# we specifically work on that path.
# aubio
